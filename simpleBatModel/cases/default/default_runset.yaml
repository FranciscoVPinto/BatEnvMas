runset: default_runset
cases_base_dir: "cases"

cases:
  - "default_case.yaml"

enabled:
  default_case: true

defaults:
  outputs_dir: "results"   # keep consistent with your run_case default
  tee: false

  # (optional) global defaults you may use later
  # solver:
  #   name: "highs"
  #   options: {}
