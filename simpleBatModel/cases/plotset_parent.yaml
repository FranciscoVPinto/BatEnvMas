plotset:
  - plotsets/plotset_pv_building8.yaml

# Maps child-plotset name -> enabled flag (must match the `plotset:` field
# inside the child YAML). Default for unlisted entries is true.
enabled:
  pv_building8: true

defaults:
  outputs_dir: results
  plots:
    per_case: true
    comparisons: true
