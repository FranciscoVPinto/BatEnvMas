plotset:
  - plotsets/plotset_alfa_deg_sweep.yaml
  - plotsets/plotset_full_horizon_sweep.yaml

enabled:
  alfa_deg_sweep: true
  full_horizon_sweep: true

defaults:
  outputs_dir: results
  plots:
    per_case: true
    comparisons: true
