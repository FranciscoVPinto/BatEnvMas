plotset: pv_building8
cases_base_dir: cases/scenarios/building8
outputs_dir: results/pv_building8

# Same case set as the matching runset. Single source of truth via glob:
# add or remove a YAML in cases/scenarios/building8/ and both pipelines
# pick it up automatically.
cases_glob: "b8_*.yaml"
enabled: {}

plots:
  per_case: true
  comparisons: true
