extends: _b8_base.yaml
case: b8_pv04_consumption_mean_export

grid:
  allow_export: true

sharing:
  mode: fixed_alpha
  normalize: false
  strict_sum_to_one: false
  fallback:
    mode: consumption_mean
    apply_when: always
